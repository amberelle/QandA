import { addTime } from 'q-and-a/helpers/add-time';
import { module, test } from 'qunit';

module('Unit | Helper | add time');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = addTime([42]);
  assert.ok(result);
});
